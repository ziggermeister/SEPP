[mypy]
python_version = 3.12
pretty = True

# If you later want stricter checks, we can tighten this.
# For now, keep friction low and focus on the active code paths.

[mypy-requests.*]
ignore_missing_imports = True

[mypy-adapters.*]
ignore_errors = True
